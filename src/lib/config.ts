// Central config — changing APP_NAME retitles every surface (titlebar,
// window chrome, welcome screen) without touching individual components.
export const APP_NAME = "csview";
export const APP_TAGLINE = "A fast, clean CSV viewer for macOS";
